# Licensing

The included font file `default_font.psf` is taken from the [console-setup-linux](https://packages.ubuntu.com/jammy/console-setup-linux) package, and was originally named `Lat15-VGA16.psf`. This package uses the same license as [keyboard-configuration](https://packages.ubuntu.com/jammy/keyboard-configuration), which states explicitly:

```
All console fonts are public domain by nature.
```
